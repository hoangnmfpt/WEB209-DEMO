import React from "react";
import s from "./Footer.module.scss";

type Props = {};

const Footer = (props: Props) => {
  return <footer className={s.footer}>Copyright 2024 by hoangnm62</footer>;
};

export default Footer;
